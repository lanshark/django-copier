# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code
in this repository.

The project's working principles, code-change behavior, Python style, and
formatting/linting rules are defined in `AGENTS.md` and apply in full to Claude
Code. It is imported below so its contents load into every session — read and
follow it.

@AGENTS.md
