{% set runner_cmd = 'make' if task_runner == 'makefile' else 'just' %}
{% set runner_file = 'Makefile' if task_runner == 'makefile' else 'Justfile' %}
# Architecture

## Status

Early development. This document is the source of truth for architecture and
conventions in {{ project_name }}. Update it as durable decisions are made, and
capture the reasoning in a feature-scoped ADR.

## Architecture Principles

- Documents are the source of truth for intent and constraints.
- Code changes are generated from documented feature requirements.
- Decisions with lasting architectural impact belong in ADRs.
- Prefer the smallest useful change and explicit contracts over broad restructuring.
- Keep concerns separate and changes reviewable.

## System Overview

{% if project_type == 'full_project' %}
{{ project_name }} is a Django project built on:

- Python {{ python_version }}, Django 5.2 LTS, PostgreSQL.
{% if use_shinobi %}
- A django-shinobi (Django Ninja) API layer with JWT auth under `apps/{{ initial_app_name }}/`.
{% else %}
- The Django admin site (no HTTP API layer in this project).
{% endif %}
{% set normalized_email_provider = email_provider|default("ses", true) %}
{% set email_provider_names = {
    "ses": "Amazon SES",
    "postmark": "Postmark",
    "mailgun": "Mailgun",
    "sendgrid": "SendGrid",
} %}
- Background work via `django-tasks`{% if use_redis %}, running on the RQ/Redis backend with a dedicated worker{% else %} (synchronous in-process backend){% endif %}.
- Email via SMTP, caught in dev by a `mailpit` service (web UI at `localhost:8025`)
  and captured in-memory during tests (`locmem` backend); production sends via
{% if normalized_email_provider in email_provider_names %}
  {{ email_provider_names[normalized_email_provider] }}
{% else %}
  {{ [].index("Unsupported email_provider: " ~ normalized_email_provider) }}
{% endif %}
  through `django-anymail`.
{% if use_vite %}
- A Pico.css + Vite frontend build pipeline (django-vite), with a starter page under
  `apps/{{ initial_app_name }}/`.
{% endif %}
- Tooling: uv, ruff, pyright, pytest, and Docker Compose.
{% else %}
{{ project_name }} is a reusable Django app built on:

- Python {{ python_version }}, Django 5.2 LTS.
- A `src/{{ package_name }}/` package installed into a host project's `INSTALLED_APPS`.
{% if use_shinobi %}
- A django-shinobi (Django Ninja) `Router` the host project mounts on its own `NinjaAPI`.
{% endif %}
- Tests against a bundled `tests/` settings module (sqlite) via pytest-django.
- Packaging with hatchling (src layout); tooling: uv, ruff, pyright, pytest.
{% endif %}

## Repository Layout

{% if project_type == 'full_project' %}
```text
.
├── AGENTS.md                # how the AI coding agent works here (+ workflow)
├── CLAUDE.md                # imports AGENTS.md for Claude Code
├── docs/
│   ├── vision.md             # product vision
│   ├── architecture.md       # this document
│   └── adr/                  # project-wide architecture decision records
├── {{ runner_file }}                 # docker compose + manage.py task wrappers
├── manage.py
├── pyproject.toml
├── docker-compose.yml
├── docker/                  # Dockerfile + entrypoint
{% if use_vite %}
├── package.json             # Vite + Pico.css frontend tooling
├── vite.config.js
├── frontend/                # Vite entry point (main.js)
{% endif %}
├── config/
│   ├── settings/            # base / dev / prod / test
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── apps/
│   └── {{ initial_app_name }}/
│       ├── AGENTS.md        # module-specific invariants
│       ├── models.py
│       ├── tasks.py
{% if use_shinobi %}
│       ├── api.py           # django-shinobi routes
│       ├── auth.py          # JWT bearer auth
│       ├── schemas.py
{% endif %}
{% if use_vite %}
│       ├── views.py         # starter page view
│       ├── urls.py
│       ├── templates/{{ initial_app_name }}/  # base.html (django-vite tags) + index.html
{% endif %}
│       └── tests/
├── scripts/
├── .agents/skills/          # Codex skill symlinks -> features/<slug>
├── .claude/skills/          # Claude Code skill symlinks -> features/<slug>
└── features/                # document-first workflow (this scaffold)
    ├── _template/           # copy (or `{{ runner_cmd }} new-feature`) to start a feature
    │   ├── feature.md        # current normative state
    │   ├── SKILL.md          # feature-specific agent guidance
    │   ├── history.md        # append-only prompt/impl/verification log
    │   └── adr/
    └── ai-driven-development/
```
{% else %}
```text
.
├── AGENTS.md                # how the AI coding agent works here (+ workflow)
├── CLAUDE.md                # imports AGENTS.md for Claude Code
├── docs/
│   ├── vision.md             # product vision
│   ├── architecture.md       # this document
│   └── adr/                  # project-wide architecture decision records
├── {{ runner_file }}                 # uv dev + packaging task wrappers
├── pyproject.toml           # hatchling build; deps; tool config
├── src/
│   └── {{ package_name }}/
│       ├── AGENTS.md        # module-specific invariants
│       ├── apps.py
│       ├── models.py
│       ├── admin.py
{% if use_shinobi %}
│       ├── api.py           # django-shinobi Router
│       ├── schemas.py
{% endif %}
│       ├── migrations/
│       └── py.typed
├── tests/                   # pytest-django settings + tests (sqlite)
├── example/                 # runnable project that installs this app
├── .agents/skills/          # Codex skill symlinks -> features/<slug>
├── .claude/skills/          # Claude Code skill symlinks -> features/<slug>
└── features/                # document-first workflow (this scaffold)
    ├── _template/           # copy (or `{{ runner_cmd }} new-feature`) to start a feature
    │   ├── feature.md        # current normative state
    │   ├── SKILL.md          # feature-specific agent guidance
    │   ├── history.md        # append-only prompt/impl/verification log
    │   └── adr/
    └── ai-driven-development/
```
{% endif %}

## Development Flow

1. Read `AGENTS.md`, `docs/vision.md`, and `docs/architecture.md`.
2. Select or create the relevant `features/<feature-slug>/` folder (from `features/_template/`).
3. Record the modifying prompt in `features/<feature-slug>/feature.md`.
4. Update requirements, acceptance criteria, and open questions.
5. Add or update ADRs for architectural choices.
6. Implement only what the documents specify.
7. Validate with focused tests or checks (`{{ runner_cmd }} test`, `{{ runner_cmd }} lint`, `{{ runner_cmd }} typecheck`).
8. Record implementation and verification notes in the feature document.

## Conventions

- Python style, formatting, and code-change behavior are defined in `AGENTS.md`.
- Lint with ruff and type-check with pyright; both must stay clean.
- Test with pytest (`pytest-django`); keep tests close to the app they cover.
{% if project_type == 'full_project' %}
- Run the project through the `{{ runner_file }}` targets (`{{ runner_cmd }} up`, `{{ runner_cmd }} migrate`, `{{ runner_cmd }} test`, …).
- Configuration is read from the environment via `python-decouple`; never commit secrets.
{% else %}
- Run tasks through the `{{ runner_file }}` targets (`{{ runner_cmd }} test`, `{{ runner_cmd }} lint`, `{{ runner_cmd }} build`, …).
- Ship migrations with the package; keep the public import surface stable and typed (`py.typed`).
{% endif %}

## Feature Folder Convention

Each feature folder contains:

- `feature.md`: current normative state — purpose, requirements, acceptance criteria, constraints.
- `SKILL.md`: instructions the AI coding agent follows when changing the feature; discoverable via `.agents/skills/` (Codex) and `.claude/skills/` (Claude Code).
- `history.md`: append-only prompt log, implementation summaries, and verification results.
- `adr/`: feature-scoped architecture decision records.

Create a new feature with
`{{ 'make new-feature name=<slug>' if task_runner == 'makefile'
    else 'just new-feature <slug>' }}`, which copies `_template/` and adds both skill
symlinks.

## ADR Convention

Use an ADR when a choice has durable consequences: runtime/framework/library/storage
choices, data-model or API-contract decisions, security or privacy decisions,
cross-feature abstractions, or significant tradeoffs. Each ADR records Title, Status,
Date, Context, Decision, Consequences, and Alternatives Considered.

ADRs live in one of two places, depending on scope:

- `docs/adr/`: project-wide decisions that span multiple features or the whole
  project (for example, runtime/framework choices, cross-cutting infrastructure,
  or project-wide security/privacy posture).
- `features/<feature-slug>/adr/`: decisions confined to that feature.

ADR numbering is independent per folder — each `adr/` folder starts its own
`0001-...` sequence.

## Open Architecture Questions

- Question 1.
