from django.http import HttpRequest, HttpResponse
from django.shortcuts import render


def index(request: HttpRequest) -> HttpResponse:
    """Render the starter page."""
    return render(request, "{{ initial_app_name }}/index.html")
