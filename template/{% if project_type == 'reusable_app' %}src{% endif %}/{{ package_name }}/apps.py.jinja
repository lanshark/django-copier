from django.apps import AppConfig


class {{ package_name | replace('_', ' ') | title | replace(' ', '') }}Config(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "{{ package_name }}"
